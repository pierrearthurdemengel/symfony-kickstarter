name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  # ---------------------------------------------------------------
  # Job 1 : Tests (reutilise le workflow CI)
  # ---------------------------------------------------------------
  tests:
    name: Lancer les tests avant deploiement
    uses: ./.github/workflows/ci.yaml

  # ---------------------------------------------------------------
  # Job 2 : Build des images Docker de production
  # ---------------------------------------------------------------
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    needs: tests

    steps:
      - name: Checkout du code source
        uses: actions/checkout@v6

      - name: Build de l'image PHP production
        run: docker build -f docker/php/Dockerfile.prod -t kickstarter-php:${{ github.ref_name }} .

      - name: Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: front/package-lock.json

      - name: Build du frontend
        run: |
          cd front
          npm ci
          npm run build

      # ---------------------------------------------------------
      # Adapter selon la plateforme de deploiement :
      # - Docker Hub : docker/login-action + docker/build-push-action
      # - GitHub Container Registry (ghcr.io)
      # - AWS ECR
      # - Google Artifact Registry
      #
      # Exemple pour GitHub Container Registry :
      #
      # - name: Login to GitHub Container Registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Push PHP image
      #   run: |
      #     docker tag kickstarter-php:${{ github.ref_name }} ghcr.io/${{ github.repository }}/php:${{ github.ref_name }}
      #     docker push ghcr.io/${{ github.repository }}/php:${{ github.ref_name }}
      # ---------------------------------------------------------

  # ---------------------------------------------------------------
  # Job 3 : Deploiement (a adapter selon l'infrastructure cible)
  # ---------------------------------------------------------------
  # deploy:
  #   name: Deploy to Production
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   environment: production
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v6
  #
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         script: |
  #           cd /opt/kickstarter
  #           git pull origin main
  #           ./scripts/deploy.sh
  #
  #     - name: Smoke test post-deploiement
  #       run: |
  #         sleep 10
  #         HEALTH=$(curl -s -o /dev/null -w "%{http_code}" https://${{ secrets.DEPLOY_DOMAIN }}/api/healthcheck)
  #         if [ "$HEALTH" != "200" ]; then
  #           echo "Healthcheck failed (HTTP $HEALTH), triggering rollback..."
  #           exit 1
  #         fi
  #         echo "Healthcheck OK (HTTP $HEALTH)"
  #
  #     - name: Rollback on failure
  #       if: failure()
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         script: |
  #           cd /opt/kickstarter
  #           git checkout HEAD~1
  #           docker compose -f docker-compose.prod.yaml up -d
  #           echo "Rollback effectue"

  # ---------------------------------------------------------------
  # Job 4 : Notification (Slack ou Discord)
  # ---------------------------------------------------------------
  notify:
    name: Notification
    needs: [build-and-push]
    runs-on: ubuntu-latest
    if: always()
    steps:
      # Exemple Slack :
      # - name: Notification Slack
      #   if: failure()
      #   uses: slackapi/slack-github-action@v1
      #   with:
      #     payload: |
      #       {
      #         "text": "Deploiement ${{ github.ref_name }} echoue sur ${{ github.repository }}"
      #       }
      #   env:
      #     SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # Exemple Discord :
      # - name: Notification Discord
      #   if: failure()
      #   uses: sarisia/actions-status-discord@v1
      #   with:
      #     webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
      #     title: "Deploiement echoue"
      #     description: "Le deploiement ${{ github.ref_name }} a echoue."
      #     color: 0xff0000

      - name: Resume du deploiement
        run: |
          echo "Tag: ${{ github.ref_name }}"
          echo "Status: ${{ needs.build-and-push.result }}"
          echo "Commit: ${{ github.sha }}"
